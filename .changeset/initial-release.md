---
"avantis-sdk": minor
---

Initial release: isomorphic viem core with full Python SDK parity (trading,
account, markets, info, referral, LP, compute, streams, MM fast path) and
`avantis-sdk/react` hooks (provider, live prices, positions, trading
mutations with SSE lifecycle, one-click-trading session keys,
permit-gasless approvals).
